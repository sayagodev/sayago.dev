import { t, type Dictionary } from 'intlayer'

const ShowVersionContent = {
  key: 'show-version',
  content: {
    label: t({
      es: 'Versión',
      en: 'Version',
    }),
  },
} satisfies Dictionary

export default ShowVersionContent
